# Built KNIF XNBs (from FSO.BrowserEffects / CI)

- `colorpoly2D.xnb` — BlazorGL KNIF (platform `b`), rebuilt from FreeSO
  `ContentSrc/Effects/colorpoly2D.fx` via `.github/workflows/kni-effects-blazor.yml`.

Stock FreeSO MGFX 11 copies live under `wwwroot/sample-content/effects/`.
