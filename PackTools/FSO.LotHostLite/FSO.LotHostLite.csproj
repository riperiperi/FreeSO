<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <RootNamespace>FSO.LotHostLite</RootNamespace>
    <AssemblyName>FSO.LotHostLite</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Same Mina the shipped sandbox protocol uses (via FSO.Common). -->
    <PackageReference Include="FSOMina.NET" Version="1.0.1" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\TSOClient\tso.simantics\FSO.SimAntics.csproj" />
  </ItemGroup>

</Project>
