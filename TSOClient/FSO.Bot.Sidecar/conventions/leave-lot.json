{
  "convention": "freeso-embodiment",
  "version": "1.0",
  "operation": "leave-lot",
  "family": "navigation",
  "description": "Leave the current lot without picking another. UI path: UCP \"Leave Lot\" button triggers `VMNetSimLeaveCmd` on the current lot.",
  "args": [],
  "produces_tags": [
    {
      "tag": "freeso:leave-lot",
      "cardinality": "exactly_one"
    }
  ],
  "antecedents": "none",
  "signing": "member_key",
  "rate_limit": {
    "max": 30,
    "per": "sender",
    "window": "1m"
  },
  "response": "sync",
  "status": "scaffold",
  "call_signatures": [
    "cf $CF leave-lot   # SCAFFOLD op — not yet implemented; handler may return ok:false or a stub. Prefer a concrete sibling op if your goal is actionable."
  ],
  "common_mistakes": [
    "This is a SCAFFOLD op — the convention is declared but the handler may be unimplemented or stubbed. Check the declaration's status field.",
    "Relying on scaffold ops mid-run will either return a deferred marker or fail — prefer a concrete sibling op, and file an rd item if the missing behaviour blocks progress."
  ],
  "perception_hints": [
    "Your current lot: perception.lot.lot_id and perception.lot.name",
    "Your position: perception.avatar.position",
    "Home detection: go-home returns already_home:true when perception.lot.lot_id matches your home lot location"
  ]
}
