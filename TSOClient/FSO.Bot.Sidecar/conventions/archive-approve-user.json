{
  "convention": "freeso-embodiment",
  "version": "1.0",
  "operation": "archive-approve-user",
  "family": "admin",
  "description": "Prerequisite: caller's moderation level strictly higher than the target's (ArchiveModerationHandler:46 refuses equal/higher). Effect: sends ArchiveModerationRequest{APPROVE_USER, EntityId=target_user_id} on the city socket. Server sets fso_users.is_verified=1 and pushes a VerificationNotification{IsVerified=true} to the target's live sessions. Cost: none. Persistent DB mutation. No response PDU. Note: EntityId here is a user_id, NOT an avatar_id (unlike kick/ban/ipban-user). Non-admin callers refused in the bot handler.",
  "args": [
    {
      "name": "target_user_id",
      "type": "uint",
      "required": true,
      "description": "fso_users.user_id of the pending user. This is NOT an avatar_id — archive moderation targets users, not Sims."
    }
  ],
  "produces_tags": [
    {
      "tag": "freeso:archive-approve-user",
      "cardinality": "exactly_one"
    }
  ],
  "antecedents": "none",
  "signing": "member_key",
  "rate_limit": {
    "max": 30,
    "per": "sender",
    "window": "1m"
  },
  "response": "sync",
  "status": "stable",
  "call_signatures": [
    "cf $CF archive-approve-user --target_user_id <user_id>                               # admin-only; target is USER_ID not avatar_id; fso_users.is_verified=1"
  ],
  "common_mistakes": [
    "EntityId / --target_user_id is a USER_ID, not an avatar_id. Look up via fso_users.user_id, not fso_avatars.avatar_id.",
    "Calling as non-admin → ok:false."
  ],
  "perception_hints": [
    "Target persist_id: perception.nearby_sims[].persist_id (for physically-present targets); other avatars need find-avatar or direct knowledge of avatar_id",
    "Your own persist_id (to avoid self-target): perception.avatar.persist_id",
    "Chat activity you may want to moderate: perception.recent_events[] kind=='chat'"
  ],
  "refuse_conditions": [
    "caller not admin → ok:false",
    "target_user_id missing or <= 0 → ok:false (NOTE: this is user_id, not avatar_id)"
  ]
}
