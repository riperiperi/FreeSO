{
  "convention": "freeso-embodiment",
  "version": "1.0",
  "operation": "tell-joke",
  "family": "social",
  "description": "Tell a joke at another Sim. Prerequisite: target Sim is present on the lot and their current pie menu exposes a 'Tell Joke' (or joke-family) entry \u2014 resolved live by the bot against the target's VM-side TTAB check. Effect: queues the Tell Joke interaction on your action queue; on success produces laughter/reaction animations on both Sims and shifts the target's relationship-to-you along the fun/friendship axis (TTAB-driven; visible in nearby_sims.relationship). Cost: ~ a minute of in-sim time; queue slot consumed; may fail server-side Verify if the target disallows (e.g. sleeping, incompatible mood).",
  "args": [
    {
      "name": "target_sim_id",
      "type": "integer",
      "required": false,
      "description": "Persist ID of the target Sim."
    },
    {
      "name": "target_object_id",
      "type": "integer",
      "required": false,
      "description": "Local ObjectID of the target Sim."
    },
    {
      "name": "queue_mode",
      "type": "string",
      "required": false,
      "default": "queue",
      "enum": [
        "queue",
        "preempt"
      ],
      "description": "How this social interaction lands on your action queue. 'queue' (default) cancels engine autopilot (Mode.Idle entries from spawn-objects and BHAVs) before enqueueing. 'preempt' cancels everything currently in your queue and runs this immediately."
    }
  ],
  "produces_tags": [
    {
      "tag": "freeso:tell-joke",
      "cardinality": "exactly_one"
    }
  ],
  "antecedents": "none",
  "signing": "member_key",
  "rate_limit": {
    "max": 20,
    "per": "sender",
    "window": "1m"
  },
  "response": "sync",
  "call_signatures": [
    "cf $CF tell-joke --target_sim_id <nearby_sims[].persist_id>                         # preferred",
    "cf $CF tell-joke --target_object_id <object_id>                                     # alternate; must resolve to a VMAvatar"
  ],
  "common_mistakes": [
    "Directed socials (be-friendly/flirt/tell-joke/be-mean/give-gift) resolve target pie-menu by ALIAS substring; if the target's pie menu has no matching entry the handler returns ok=false with the list of available names \u2014 read the error to pick a different verb.",
    "Passing --target_object_id for a non-avatar object silently fails these socials (they require VMAvatar target).",
    "Speak rejects leading '/' \u2014 that prefix is reserved for admin chat commands; use admin-chat-command instead."
  ],
  "perception_hints": [
    "Target persist_id: perception.nearby_sims[].persist_id",
    "Target's current action (to judge if they're busy): perception.nearby_sims[].current_action",
    "Your social motive (why this matters): perception.motives.social.value",
    "Existing relationships: perception.relationships[].with_persist_id / .friendly / .daily",
    "Each queue entry's .mode tells you what's there: 'normal' = deliberate (yours or another Sim's social), 'idle' = engine autopilot. queue_mode='queue' clears 'idle' entries; queue_mode='preempt' clears all."
  ],
  "refuse_conditions": [
    "no live avatar \u2192 ok:false",
    "no target_sim_id/target_object_id \u2192 ok:false",
    "target not a Sim \u2192 ok:false",
    "no matching pie-menu entry \u2192 ok:false with available-names list"
  ]
}
