{
  "convention": "freeso-embodiment",
  "version": "1.0",
  "operation": "set-ignore",
  "family": "social",
  "description": "Add or remove another avatar from your server-side ignore list. An ignored avatar's IM and chat to you are suppressed server-side; the effect also shows up as a `DataServiceWrapperPDU` MyAvatar update.",
  "args": [],
  "produces_tags": [
    {
      "tag": "freeso:set-ignore",
      "cardinality": "exactly_one"
    }
  ],
  "antecedents": "none",
  "signing": "member_key",
  "rate_limit": {
    "max": 30,
    "per": "sender",
    "window": "1m"
  },
  "response": "sync",
  "status": "scaffold",
  "call_signatures": [
    "cf $CF set-ignore   # SCAFFOLD op — not yet implemented; handler may return ok:false or a stub. Prefer a concrete sibling op if your goal is actionable."
  ],
  "common_mistakes": [
    "This is a SCAFFOLD op — the convention is declared but the handler may be unimplemented or stubbed. Check the declaration's status field.",
    "Relying on scaffold ops mid-run will either return a deferred marker or fail — prefer a concrete sibling op, and file an rd item if the missing behaviour blocks progress."
  ],
  "perception_hints": [
    "Target persist_id: perception.nearby_sims[].persist_id",
    "Target's current action (to judge if they're busy): perception.nearby_sims[].current_action",
    "Your social motive (why this matters): perception.motives.social.value",
    "Existing relationships: perception.relationships[].with_persist_id / .friendly / .daily"
  ]
}
