{
  "convention": "freeso-embodiment",
  "version": "1.0",
  "operation": "place-from-inventory",
  "family": "build-buy-catalog",
  "description": "Place an object from your personal inventory into the lot at (x,y,level) facing dir. Different from buy-object because no transaction runs; the object's persist-id is preserved. Prerequisite: you are the lot owner (handler-side VMTSOLotState.OwnerID gate). The referenced object must be present in your inventory. Effect: the object appears in nearby_objects; your inventory count decreases by 1. Cost: none (no money moves). Wire: VMNetPlaceInventoryCmd on the lot Aries socket.",
  "args": [
    {
      "name": "object_persist_id",
      "type": "uint64",
      "required": true
    },
    {
      "name": "x",
      "type": "int64",
      "required": true
    },
    {
      "name": "y",
      "type": "int64",
      "required": true
    },
    {
      "name": "level",
      "type": "int64",
      "required": false
    },
    {
      "name": "dir",
      "type": "int64",
      "required": false
    },
    {
      "name": "mode",
      "type": "string",
      "required": false
    }
  ],
  "produces_tags": [
    {
      "tag": "freeso:place-from-inventory",
      "cardinality": "exactly_one"
    }
  ],
  "antecedents": "none",
  "signing": "member_key",
  "rate_limit": {
    "max": 30,
    "per": "sender",
    "window": "1m"
  },
  "response": "sync",
  "status": "implemented",
  "call_signatures": [
    "cf $CF place-from-inventory --object_persist_id <pid> --x <subtile_x> --y <subtile_y>   # place an inventory item; owner-only",
    "cf $CF place-from-inventory --object_persist_id <pid> --x <x> --y <y> --level 2 --dir 128  # upper floor"
  ],
  "common_mistakes": [
    "Calling as non-lot-owner → ok:false.",
    "object_persist_id = 0 or missing → ok:false.",
    "x/y are SUBTILE coords, not tile coords."
  ],
  "perception_hints": [
    "Your balance (can you afford the object?): perception.balance",
    "Am I the owner? perception.lot.owner_is_me — owner-only ops refuse otherwise",
    "Existing object_id for move/delete/upgrade: perception.nearby_objects[].object_id",
    "Free tile to place on: inspect perception.nearby_objects[] to find unoccupied tiles; positions are in tile coords, multiply by 16 for subtile x/y"
  ],
  "refuse_conditions": [
    "caller is not lot owner (for buy/move/delete/place-from-inventory) → returns ok:false with 'caller is not lot owner (owner_id=..., me=...)'",
    "caller is not object owner (for send-to-inventory/list-for-sale/upgrade) → returns ok:false with 'caller is not the object owner (object_owner=..., me=...)'",
    "required args missing (guid, x, y, target_object_id, target_object_persist_id) → returns ok:false with specific missing-arg error"
  ]
}
