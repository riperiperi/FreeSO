namespace FSO.LotView.Model
{
    /// <summary>
    /// Geometry grouped by some key (usually texture id or name) potentially with related indices or data that can be uses to remove or modify a single element later.
    /// 
    /// </summary>
    public class GroupedGeometry
    {
    }
}
